---
title: "Create a .env file with all your Chainstack endpoints with Python"
description: "Learn how to automatically generate a .env file listing all your Chainstack endpoints with Python."
---

import { Button } from '/snippets/button.mdx';

### In this Recipe

<Steps>
  <Step>
    Overview
  </Step>
  <Step>
    Prerequisites
  </Step>
  <Step>
    Environment setup
  </Step>
</Steps>

<RequestExample>
```py get_endpoints.py
import requests
import os
import logging
from dotenv import load_dotenv
from web3 import Web3
from typing import Optional, Dict, Any

# Load environment variables
load_dotenv()

# Constants
CHAINSTACK_API_KEY = os.getenv('CHAINSTACK_API_KEY')
OUTPUT_FILE_NAME = 'rpc.env'
```
</RequestExample>

<CardGroup cols={1}>
<Card title="How to get ERC-20 token transfer logs using ethers.js" href="#" icon="books" iconType="solid">

This recipe shows you how to use the Ethers library with a Chainstack Ethereum node to retrieve transfer logs for an ERC-20 token.
<br/>
7 Steps

<Button href="#">Open Recipe</Button>
</Card>

<Card title="Extract 'randao' value from the Ethereum Beacon chain using the block details method" href="/recipes/extract-randao-value-from-the-ethereum-beacon-chain-using-the-block-details-method-1" icon="code" iconType="solid">

This script extracts the 'randao' information from the Beacon chain block details in JavaScript using the Axios package.
<br/>
6 Steps

<Button href="/recipes/extract-randao-value-from-the-ethereum-beacon-chain-using-the-block-details-method-1">Open Recipe</Button>
</Card>

<Card title="Identify if a block has been included in the main chain or was forked" href="#" icon="link" iconType="solid">

This script uses web3.js to evaluate whether a specific block within a blockchain network has been integrated into the main chain or if it was a result of a reorg.

6 Steps

<Button href="#">Open Recipe</Button>
</Card>
</CardGroup>
