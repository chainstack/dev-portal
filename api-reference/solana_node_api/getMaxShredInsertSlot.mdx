---
title: getMaxShredInsertSlot | Solana
openapi: /openapi/solana_node_api/getMaxShredInsertSlot.json POST /9de47db917d4f69168e3fed02217d15b
---

# Solana `getMaxShredInsertSlot` method

The Solana `getMaxShredInsertSlot` method returns the highest slot that shreds have been received for and inserted into blockstore.

This method provides the highest slot that the node has received shreds for, which is useful for understanding the node's progress in processing the ledger.

<Check>
**Get you own node endpoint today**

[Start for free](https://console.chainstack.com/) and get your app to production levels immediately. No credit card required.

You can sign up with your GitHub, X, Google, or Microsoft account.
</Check>


## Parameters

This method does not require any parameters.

## Response

* `value` — the highest slot that shreds have been received for and inserted into blockstore.

## Use case

A practical use case for `getMaxShredInsertSlot` is to track the node's progress in processing the ledger. This can be useful for network monitoring tools or other applications that need to display or make use of this information.

## Try the `getMaxShredInsertSlot` RPC method yourself