---
title: "eth_getStorageAt | Robinhood"
openapi: /openapi/robinhood_node_api/accounts_info/eth_getStorageAt.json POST /
description: "Robinhood Chain API method that returns the value from a storage position at a given address. Available on Robinhood Chain via Chainstack JSON-RPC nodes."
---

Robinhood Chain API method that returns the value from a storage position at a given address.

## Parameters

* `address` — the address to get storage from
* `quantity` — the position in the storage
* `quantity or tag` — the block number in hex format or block tag (`latest`, `earliest`, `pending`, `safe`, `finalized`)

## Response

* `data` — the value at this storage position

## `eth_getStorageAt` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_getStorageAt", ["0x00000000000000000000000000000000000a4b05", "0x0", "latest"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_getStorageAt", ["0x00000000000000000000000000000000000a4b05", "0x0", "latest"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_getStorageAt",
    "params": ["0x00000000000000000000000000000000000a4b05", "0x0", "latest"],
    "id": 1
  }'
```
</CodeGroup>
