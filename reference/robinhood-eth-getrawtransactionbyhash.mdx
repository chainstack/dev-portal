---
title: "eth_getRawTransactionByHash | Robinhood"
openapi: /openapi/robinhood_node_api/transaction_info/eth_getRawTransactionByHash.json POST /
description: "Robinhood Chain API method that returns the raw transaction data by transaction hash. data — the hash of the transaction. Robinhood Chain via Chainstack."
---

Robinhood Chain API method that returns the raw transaction data by transaction hash.

## Parameters

* `data` — the hash of the transaction

## Response

* `data` — the raw transaction data, or `null` when no transaction was found

## `eth_getRawTransactionByHash` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_getRawTransactionByHash", ["0x73b5269af660acf6f1074c5f9f312d415c359f2e5f5f67e95b369d0fbd3bd6b8"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_getRawTransactionByHash", ["0x73b5269af660acf6f1074c5f9f312d415c359f2e5f5f67e95b369d0fbd3bd6b8"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_getRawTransactionByHash",
    "params": ["0x73b5269af660acf6f1074c5f9f312d415c359f2e5f5f67e95b369d0fbd3bd6b8"],
    "id": 1
  }'
```
</CodeGroup>
