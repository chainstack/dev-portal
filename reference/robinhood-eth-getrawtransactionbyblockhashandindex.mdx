---
title: "eth_getRawTransactionByBlockHashAndIndex | Robinhood"
openapi: /openapi/robinhood_node_api/transaction_info/eth_getRawTransactionByBlockHashAndIndex.json POST /
description: "Robinhood Chain API method that returns the raw transaction data by block hash and transaction index. Available on Robinhood Chain via Chainstack JSON-RPC nodes."
---

Robinhood Chain API method that returns the raw transaction data by block hash and transaction index.

## Parameters

* `data` — the hash of the block
* `quantity` — the transaction index position

## Response

* `data` — the raw transaction data, or `null` when no transaction was found

## `eth_getRawTransactionByBlockHashAndIndex` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_getRawTransactionByBlockHashAndIndex", ["0xf4aab7a559489b816aca197b9a9315b5363a9932c52c3e8bfcf4152588ee096a", "0x0"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_getRawTransactionByBlockHashAndIndex", ["0xf4aab7a559489b816aca197b9a9315b5363a9932c52c3e8bfcf4152588ee096a", "0x0"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_getRawTransactionByBlockHashAndIndex",
    "params": ["0xf4aab7a559489b816aca197b9a9315b5363a9932c52c3e8bfcf4152588ee096a", "0x0"],
    "id": 1
  }'
```
</CodeGroup>
