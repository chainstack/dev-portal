---
title: "eth_blobBaseFee | Robinhood"
openapi: /openapi/robinhood_node_api/gas_data/eth_blobBaseFee.json POST /
description: "Robinhood Chain API method that returns the base fee for blob gas in Wei. eth_blobBaseFee JSON-RPC method available on the Robinhood Chain blockchain via Chainstack."
---

Robinhood Chain API method that returns the base fee for blob gas in Wei.

## Parameters

None

## Response

* `quantity` — the current blob base fee in Wei

## `eth_blobBaseFee` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_blobBaseFee", []);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_blobBaseFee", [])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_blobBaseFee",
    "params": [],
    "id": 1
  }'
```
</CodeGroup>
