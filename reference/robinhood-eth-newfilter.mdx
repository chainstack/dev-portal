---
title: "eth_newFilter | Robinhood"
openapi: /openapi/robinhood_node_api/filter_handling/eth_newFilter.json POST /
description: "Robinhood Chain API method that creates a filter object based on filter options to notify when the state changes (logs). Robinhood Chain via Chainstack."
---

Robinhood Chain API method that creates a filter object based on filter options to notify when the state changes (logs).

## Parameters

* `object` — the filter options with fields like `fromBlock`, `toBlock`, `address`, `topics`

## Response

* `quantity` — a filter ID

## `eth_newFilter` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_newFilter", [{"fromBlock": "0xc3007d", "toBlock": "0xc3007d"}]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_newFilter", [{"fromBlock": "0xc3007d", "toBlock": "0xc3007d"}])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_newFilter",
    "params": [{"fromBlock": "0xc3007d", "toBlock": "0xc3007d"}],
    "id": 1
  }'
```
</CodeGroup>
