---
title: "web3_clientVersion | Robinhood"
openapi: /openapi/robinhood_node_api/client_info/web3_clientVersion.json POST /
description: "Robinhood Chain API method that returns the current client version. web3_clientVersion JSON-RPC method available on the Robinhood Chain blockchain via Chainstack."
---

Robinhood Chain API method that returns the current client version.

## Parameters

None

## Response

* `string` — the current client version

## `web3_clientVersion` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("web3_clientVersion", []);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("web3_clientVersion", [])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "web3_clientVersion",
    "params": [],
    "id": 1
  }'
```
</CodeGroup>
