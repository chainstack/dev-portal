---
title: "eth_getTransactionByBlockHashAndIndex | Robinhood"
openapi: /openapi/robinhood_node_api/transaction_info/eth_getTransactionByBlockHashAndIndex.json POST /
description: "Robinhood Chain API method that returns information about a transaction by block hash and transaction index position. Robinhood Chain via Chainstack."
---

Robinhood Chain API method that returns information about a transaction by block hash and transaction index position.

## Parameters

* `data` — the hash of the block
* `quantity` — the transaction index position

## Response

* `object` — a transaction object, or `null` when no transaction was found

## `eth_getTransactionByBlockHashAndIndex` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_getTransactionByBlockHashAndIndex", ["0xf4aab7a559489b816aca197b9a9315b5363a9932c52c3e8bfcf4152588ee096a", "0x0"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_getTransactionByBlockHashAndIndex", ["0xf4aab7a559489b816aca197b9a9315b5363a9932c52c3e8bfcf4152588ee096a", "0x0"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_getTransactionByBlockHashAndIndex",
    "params": ["0xf4aab7a559489b816aca197b9a9315b5363a9932c52c3e8bfcf4152588ee096a", "0x0"],
    "id": 1
  }'
```
</CodeGroup>
