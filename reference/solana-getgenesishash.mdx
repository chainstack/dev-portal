---
title: getGenesisHash | Solana
openapi: /openapi/solana_node_api/getGenesisHash.json POST /9de47db917d4f69168e3fed02217d15b
description: "The Solana getGenesisHash method returns the genesis hash for the current network. Available on Solana via Chainstack JSON-RPC nodes."
---

# Solana `getGenesisHash` method

The Solana `getGenesisHash` method returns the genesis hash for the current network.

This method provides the genesis hash, which is a unique identifier for the network's genesis block. It is useful for distinguishing between different Solana networks.

<Visibility for="humans">
<Check>
**Get your own node endpoint today**

[Start for free](https://console.chainstack.com/) and get your app to production levels immediately. No credit card required.

You can sign up with your GitHub, X, Google, or Microsoft account.
</Check>
</Visibility>


## Parameters

This method does not require any parameters.

## Response

* `value` — the genesis hash for the current network.

## Use case

A practical use case for `getGenesisHash` is to distinguish between different Solana networks. This can be useful for wallets or other applications that need to display or make use of this information.
