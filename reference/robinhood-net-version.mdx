---
title: "net_version | Robinhood"
openapi: /openapi/robinhood_node_api/client_info/net_version.json POST /
description: "Robinhood Chain API method that returns the current network ID. net_version JSON-RPC method available on the Robinhood Chain blockchain via Chainstack."
---

Robinhood Chain API method that returns the current network ID.

## Parameters

None

## Response

* `string` — the current network ID

## `net_version` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("net_version", []);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("net_version", [])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "net_version",
    "params": [],
    "id": 1
  }'
```
</CodeGroup>
