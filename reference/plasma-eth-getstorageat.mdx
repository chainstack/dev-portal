---
title: eth_getStorageAt | Plasma
openapi: /openapi/plasma_node_api/accounts_info/eth_getStorageAt.json POST /be84184cd12967be5a65d06651c60085
---

Plasma API method that returns the value from a storage position at a given address.

## Parameters

* `address` — the address to get storage from
* `quantity` — the position in the storage
* `quantity or tag` — the block number in hex format or block tag (`latest`, `earliest`, `pending`, `safe`, `finalized`)

## Response

* `data` — the value at this storage position

## `eth_getStorageAt` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_getStorageAt", [
    "0x742d35Cc6634C0532925a3b844Bc454e4438f44e",
    "0x0",
    "latest"
  ]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

# Make the call
# Add your parameters here
# result = web3.eth.<method_name>(...)
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_getStorageAt",
    "params": [],
    "id": 1
  }'
```
</CodeGroup>
