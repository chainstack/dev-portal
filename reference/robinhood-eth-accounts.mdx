---
title: "eth_accounts | Robinhood"
openapi: /openapi/robinhood_node_api/chain_info/eth_accounts.json POST /
description: "Robinhood Chain API method that returns a list of addresses owned by the client. Chainstack supports eth_accounts on Robinhood Chain JSON-RPC nodes."
---

Robinhood Chain API method that returns a list of addresses owned by the client.

## Parameters

None

## Response

* `array` — an array of addresses owned by the client

## `eth_accounts` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_accounts", []);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_accounts", [])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_accounts",
    "params": [],
    "id": 1
  }'
```
</CodeGroup>
