---
title: eth_getTransactionByBlockHashAndIndex | Arc
openapi: /openapi/arc_node_api/transaction_info/eth_getTransactionByBlockHashAndIndex.json POST /
description: "Arc API method that returns information about a transaction by block hash and transaction index position. Chainstack Arc reference."
---

Arc API method that returns information about a transaction by block hash and transaction index position.

<Visibility for="humans">
<Check>
**Get your own node endpoint today**

[Start for free](https://console.chainstack.com/) and get your app to production levels immediately. No credit card required.

You can sign up with your GitHub, X, Google, or Microsoft account.
</Check>
</Visibility>

## Parameters

* `blockHash` — the hash of the block
* `transactionIndex` — the transaction index position (hex)

## Response

* `result` — the transaction object, or `null` if not found:
  * `hash` — transaction hash
  * `nonce` — sender's transaction count
  * `blockHash` — hash of the containing block
  * `blockNumber` — block number
  * `transactionIndex` — index in the block
  * `from` — sender address
  * `to` — recipient address (null for contract creation)
  * `value` — value transferred in wei
  * `gas` — gas limit
  * `gasPrice` — gas price
  * `input` — transaction data
  * `v`, `r`, `s` — signature values

## `eth_getTransactionByBlockHashAndIndex` code examples

<CodeGroup>
```javascript ethers.js
const ethers = require('ethers');
const NODE_URL = "CHAINSTACK_NODE_URL";
const provider = new ethers.JsonRpcProvider(NODE_URL);

const getTransactionByIndex = async (blockHash, index) => {
    const tx = await provider.send("eth_getTransactionByBlockHashAndIndex", [
      blockHash,
      index
    ]);

    if (tx) {
      console.log(`Transaction at index ${parseInt(index, 16)}:`);
      console.log(`  Hash: ${tx.hash}`);
      console.log(`  From: ${tx.from}`);
      console.log(`  To: ${tx.to || 'Contract Creation'}`);
      console.log(`  Value: ${ethers.formatEther(tx.value)} ETH`);
    } else {
      console.log("Transaction not found");
    }
  };

getTransactionByIndex(
  "0xa86891cc8ae2e3efe67b21e24f8f852e76a0875892e9e8c9a4c77aab8e2761cc",
  "0x0"
);
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_getTransactionByBlockHashAndIndex", [
    "0xa86891cc8ae2e3efe67b21e24f8f852e76a0875892e9e8c9a4c77aab8e2761cc",
    "0x0"
])

tx = result['result']
if tx:
    print(f"Transaction at index 0:")
    print(f"  Hash: {tx['hash']}")
    print(f"  From: {tx['from']}")
    print(f"  To: {tx.get('to', 'Contract Creation')}")
    print(f"  Value: {web3.from_wei(int(tx['value'], 16), 'ether')} ETH")
else:
    print("Transaction not found")
```

```bash cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  -d '{
    "jsonrpc": "2.0",
    "method": "eth_getTransactionByBlockHashAndIndex",
    "params": ["0xa86891cc8ae2e3efe67b21e24f8f852e76a0875892e9e8c9a4c77aab8e2761cc", "0x0"],
    "id": 1
  }'
```
</CodeGroup>
