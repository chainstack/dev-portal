---
title: debug_getRawTransaction | Arc
openapi: /openapi/arc_node_api/debug_and_trace/debug_getRawTransaction.json POST /
description: "Arc API method that returns the RLP-encoded transaction data for a given transaction hash. transactionHash — the hash of the transaction."
---

Arc API method that returns the RLP-encoded transaction data for a given transaction hash.

<Visibility for="humans">
<Check>
**Get your own node endpoint today**

[Start for free](https://console.chainstack.com/) and get your app to production levels immediately. No credit card required.

You can sign up with your GitHub, X, Google, or Microsoft account.
</Check>
</Visibility>

## Parameters

* `transactionHash` — the hash of the transaction

## Response

* `result` — the RLP-encoded transaction data as a hex string, or `null` if the transaction is not found.

## `debug_getRawTransaction` code examples

<CodeGroup>
```bash cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc": "2.0", "method": "debug_getRawTransaction", "params": ["0xfc48f7c70e8ffaa46b1110dc43f32e90f65434a0949edab384d066c820c57ee2"], "id": 1}'
```
</CodeGroup>
