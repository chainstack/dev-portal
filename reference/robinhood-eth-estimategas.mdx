---
title: "eth_estimateGas | Robinhood"
openapi: /openapi/robinhood_node_api/execute_transactions/eth_estimateGas.json POST /
description: "Robinhood Chain API method that generates and returns an estimate of how much gas is necessary for the transaction to complete. Robinhood Chain via Chainstack."
---

Robinhood Chain API method that generates and returns an estimate of how much gas is necessary for the transaction to complete.

## Parameters

* `object` — the transaction call object with fields like `from`, `to`, `gas`, `gasPrice`, `value`, `data`

## Response

* `quantity` — the amount of gas used

## `eth_estimateGas` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_estimateGas", [{"to": "0x00000000000000000000000000000000000a4b05", "data": "0x"}]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_estimateGas", [{"to": "0x00000000000000000000000000000000000a4b05", "data": "0x"}])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_estimateGas",
    "params": [{"to": "0x00000000000000000000000000000000000a4b05", "data": "0x"}],
    "id": 1
  }'
```
</CodeGroup>
