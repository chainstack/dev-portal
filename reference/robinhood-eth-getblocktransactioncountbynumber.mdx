---
title: "eth_getBlockTransactionCountByNumber | Robinhood"
openapi: /openapi/robinhood_node_api/blocks_info/eth_getBlockTransactionCountByNumber.json POST /
description: "Robinhood Chain API method that returns the number of transactions in a block by block number. eth_getBlockTransactionCountByNumber on Robinhood Chain via Chainstack."
---

Robinhood Chain API method that returns the number of transactions in a block by block number.

## Parameters

* `quantity or tag` — the block number in hex format or block tag (`latest`, `earliest`, `pending`, `safe`, `finalized`)

## Response

* `quantity` — the number of transactions in the block

## `eth_getBlockTransactionCountByNumber` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_getBlockTransactionCountByNumber", ["0xc3007d"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_getBlockTransactionCountByNumber", ["0xc3007d"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_getBlockTransactionCountByNumber",
    "params": ["0xc3007d"],
    "id": 1
  }'
```
</CodeGroup>
