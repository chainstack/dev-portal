---
title: eth_accounts | Arc
openapi: /openapi/arc_node_api/chain_info/eth_accounts.json POST /
description: "Arc API method that returns a list of addresses owned by the node. Since Arc nodes do not manage private keys, this always returns an empty array."
---

Arc API method that returns a list of addresses owned by the node. Since Arc nodes do not manage private keys, this always returns an empty array.

<Visibility for="humans">
<Check>
**Get your own node endpoint today**

[Start for free](https://console.chainstack.com/) and get your app to production levels immediately. No credit card required.

You can sign up with your GitHub, X, Google, or Microsoft account.
</Check>
</Visibility>

## Parameters

* `none`

## Response

* `result` — an array of addresses owned by the node. On Arc, this always returns an empty array since the node does not manage accounts.

## `eth_accounts` code examples

<CodeGroup>
```bash cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  -d '{"jsonrpc": "2.0", "method": "eth_accounts", "params": [], "id": 1}'
```
</CodeGroup>
