---
title: "eth_getBlockReceipts | Robinhood"
openapi: /openapi/robinhood_node_api/blocks_info/eth_getBlockReceipts.json POST /
description: "Robinhood Chain API method that returns all transaction receipts for a given block. quantity or tag — the block number in hex format or block tag (latest, earliest."
---

Robinhood Chain API method that returns all transaction receipts for a given block.

## Parameters

* `quantity or tag` — the block number in hex format or block tag (`latest`, `earliest`, `pending`, `safe`, `finalized`)

## Response

* `array` — an array of receipt objects for all transactions in the block

## `eth_getBlockReceipts` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_getBlockReceipts", ["0xc3007d"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_getBlockReceipts", ["0xc3007d"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_getBlockReceipts",
    "params": ["0xc3007d"],
    "id": 1
  }'
```
</CodeGroup>
