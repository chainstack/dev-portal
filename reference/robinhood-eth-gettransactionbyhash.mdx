---
title: "eth_getTransactionByHash | Robinhood"
openapi: /openapi/robinhood_node_api/transaction_info/eth_getTransactionByHash.json POST /
description: "Robinhood Chain API method that returns information about a transaction by transaction hash. data — the hash of the transaction. On Robinhood Chain."
---

Robinhood Chain API method that returns information about a transaction by transaction hash.

## Parameters

* `data` — the hash of the transaction

## Response

* `object` — a transaction object, or `null` when no transaction was found

## `eth_getTransactionByHash` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_getTransactionByHash", ["0x73b5269af660acf6f1074c5f9f312d415c359f2e5f5f67e95b369d0fbd3bd6b8"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_getTransactionByHash", ["0x73b5269af660acf6f1074c5f9f312d415c359f2e5f5f67e95b369d0fbd3bd6b8"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_getTransactionByHash",
    "params": ["0x73b5269af660acf6f1074c5f9f312d415c359f2e5f5f67e95b369d0fbd3bd6b8"],
    "id": 1
  }'
```
</CodeGroup>
