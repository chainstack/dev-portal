---
title: "debug_chaindbProperty | Robinhood"
openapi: /openapi/robinhood_node_api/debug_and_trace/debug_chaindbProperty.json POST /
description: "Robinhood Chain API method that returns a low-level property of the node's underlying key-value database. It is an operational debug method used for node diagnostics."
---

Robinhood Chain API method that returns a low-level property of the node's underlying key-value database. It is an operational debug method used for node diagnostics.

## Parameters

* `property` — the database property name to read

## Response

* `string` — the value of the requested database property

## `debug_chaindbProperty` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("debug_chaindbProperty", ["leveldb.stats"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("debug_chaindbProperty", ["leveldb.stats"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "debug_chaindbProperty",
    "params": ["leveldb.stats"],
    "id": 1
  }'
```
</CodeGroup>
