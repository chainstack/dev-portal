---
title: "eth_createAccessList | Robinhood"
openapi: /openapi/robinhood_node_api/execute_transactions/eth_createAccessList.json POST /
description: "Robinhood Chain API method that creates an EIP-2930 access list for a transaction. Reference for eth_createAccessList on Robinhood Chain via Chainstack."
---

Robinhood Chain API method that creates an EIP-2930 access list for a transaction.

<Note>
When called against a block older than the latest ~128 blocks, this method is treated as an archive request (2 RUs instead of 1 RU). See [request units](/docs/request-units#archive-state-methods).
</Note>

## Parameters

* `object` — the transaction call object
* `quantity or tag` — the block number in hex format or block tag (`latest`, `earliest`, `pending`, `safe`, `finalized`)

## Response

* `object` — an object with `accessList` and `gasUsed`

## `eth_createAccessList` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("eth_createAccessList", [{"to": "0x00000000000000000000000000000000000a4b05", "data": "0x"}, "latest"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("eth_createAccessList", [{"to": "0x00000000000000000000000000000000000a4b05", "data": "0x"}, "latest"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "eth_createAccessList",
    "params": [{"to": "0x00000000000000000000000000000000000a4b05", "data": "0x"}, "latest"],
    "id": 1
  }'
```
</CodeGroup>
