---
title: "web3_sha3 | Robinhood"
openapi: /openapi/robinhood_node_api/client_info/web3_sha3.json POST /
description: "Robinhood Chain API method that returns Keccak-256 (not the standardized SHA3-256) of the given data. data — the data to convert into a SHA3 hash."
---

Robinhood Chain API method that returns Keccak-256 (not the standardized SHA3-256) of the given data.

## Parameters

* `data` — the data to convert into a SHA3 hash

## Response

* `data` — the SHA3 result of the given string

## `web3_sha3` code examples

<CodeGroup>
```javascript ethers.js
const { JsonRpcProvider } = require("ethers");

const provider = new JsonRpcProvider("CHAINSTACK_NODE_URL");

async function call() {
  const result = await provider.send("web3_sha3", ["0x68656c6c6f"]);
  console.log(result);
}

call();
```

```python web3.py
from web3 import Web3

node_url = "CHAINSTACK_NODE_URL"
web3 = Web3(Web3.HTTPProvider(node_url))

result = web3.provider.make_request("web3_sha3", ["0x68656c6c6f"])
print(result)
```

```shell cURL
curl -X POST "CHAINSTACK_NODE_URL" \
  -H "Content-Type: application/json" \
  --data '{
    "jsonrpc": "2.0",
    "method": "web3_sha3",
    "params": ["0x68656c6c6f"],
    "id": 1
  }'
```
</CodeGroup>
